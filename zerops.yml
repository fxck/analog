docsstage:
  build:
    base: [ nodejs@16 ]
    build:
      - yarn
      - yarn build
    deploy: [ 'dist/apps/docs-app/~' ]

appstage:
  build:
    base: [ nodejs@16 ]
    build:
      - yarn
      - yarn build
    deploy: [ 'dist/apps/analog-app/~' ]

docsprod:
  build:
    base: [ nodejs@16 ]
    build:
      - yarn
      - yarn build
    deploy: [ 'dist/apps/docs-app/~' ]

appprod:
  build:
    base: [ nodejs@16 ]
    build:
      - yarn
      - yarn build
    deploy: [ 'dist/apps/analog-app/~' ]
